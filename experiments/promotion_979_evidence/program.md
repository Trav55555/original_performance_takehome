# Source-only 979 promotion

User authorized automatic source-only discovery and full promotion verification after the eight-case experiment. Work in the detached /tmp/perf-promote979.repo worktree, based on0067321. Preserve original workspace, historical experiments and frozen tests/simulator. No commit, push, installation, cloud work or solver query.

Implement one bounded deterministic production policy. Retain existing cache/selector discovery. Select one generated refinement seed using the existing32/64-cycle necessary-window preflight ranking at one cycle below the best native span. This is a seed heuristic, not a bound on global reordering. Apply one backward/forward pass in each of two tie orders. From the better legal schedule, derive the two latest terminal store blocks with block identity as a stable tie break. Evaluate their singleton and paired final-hash rewrites under both orders. No literal winning site/block lists, ordinal edits, saved timings, or experimental configurations in production.

Count all eight or fewer additional schedules against the existing4096-score cap before graph construction. Reject scratch>1536 before lowering or execution, including naturally overflowing retimed schedules. Select the best complete legal result, require<=979, and fail explicitly if the finite policy cannot meet it. No solver fallback or budget expansion. Preserve source-only and named-oracle-blocked generation. Keep existing pinned dependency/assertion requirements in this promotion.

First run small executable scheduling and failure-sensitive controls. Then one genuinely cold public KernelBuilder build and the full suite: independent logical/physical checks,100 full-width inputs,100 supplemental inputs,five patterns,nine frozen tests,other shapes/legacy/explicit overrides/cached-copy isolation,pause/store completion,performance and corruption controls, and two isolated source-only builds with hash seeds0 and17. The three cold builds must agree. Record build timings separately from warm calls. Never lower or execute an over-scratch candidate. No saved experimental plan is a production input.

Use the existing pinned interpreter. Compilation cost is secondary. Launch one resumable-observation full-verification process with status/logs and immutable source hashes. Do not restart a failed cold build automatically. A test-driver failure may be investigated without new discovery or candidate search; report it rather than spending an undeclared rerun. Source changes after a completed cold build invalidate its promotion evidence and need an explicit new verification decision.

After all gates pass, synchronize only the reviewed implementation/verifier/docs files into the original working tree, preserving unrelated untracked research. Write the receipt and results. No commit or push. If a gate fails, leave production unchanged and report the exact blocker.
