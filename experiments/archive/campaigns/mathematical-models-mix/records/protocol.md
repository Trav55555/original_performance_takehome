# Research program: matched mathematical-model combinations

Authorization: user asked to analyze the five-arm results, then test whether mixing and matching is useful. This is a NEW bounded program, not a restart or reuse of expired slots. Preserve production d3544dc, every earlier workspace and all frozen tests. No solver, package install, cloud job, commit, push or production edit.

## Analysis and hypotheses

The earlier strict isolated-improvement gate excluded all combinations, despite prior project evidence that ties and small regressions sometimes combine profitably. Test interaction directly rather than require each component to win alone.

A18 removes one flow instruction and advances the last gather but ties981 with extra scratch. A16 uses the same depth-three coefficient basis at a neighboring walker, so the pair may amortize setup. A09/A21 affect disjoint depth-four tiles with smaller standalone scratch. C02/C06 project parity at late walkers and tie or nearly tie. C01 is a slower, carry-aware radix alternative with a different dependency path. Weak wavefront/Morton/bit-reversed priorities can change where that work is issued. Max-plus compaction can remove holes created by NEW graphs, even though it did not improve the old one.

Exclude broad recursive B plus pure scheduling from this program: its retained arithmetic credit bound is at least988. This is not a rejection of B after new operation elimination or cache reselection. Do not combine conflicting tensor tiles or incompatible global branch modes. Do not allow full recursive conversion to silently override a tile.

One additional controlled factor S reuses structurally identical existing scalar differences and broadcasts during tensor coefficient construction. It changes neither lookup algebra nor unrelated compiler operations. Compare S on/off on the same single and double tiles before combining it with parity work. No algebraic expansion search or whole-IR CSE.

## Fixed families and budgets

Control: four constructions/replays, same native equality, archived published980 fresh allocation, and executable event-toy controls as the previous program. Saved timing is a control only, never an input to candidate selection.

Single:12 matched current-run components: A18,A09,A16,A21; C02,C06,C01; D01,D10,D16; E01,E05. Must reproduce prior cycles/scratch/digest before combinations proceed.

Pair:40 cases: four A by three C (12), four A by three D (12), three C by three D (9), all six pairs of distinct A tiles (6), and C02+C06 (1). Keep all feasible outcomes, not only individually improving components.

Sharing:10 cases: S on the four individual A cases and their six pairs. S-off counterparts are already measured. The S-only baseline is a structural no-op because no tensor helper is called; original/native equality tests remain mandatory.

Sharing-cross:6 cases: the two best feasible S-enabled single tiles by (cycles,scratch,stable ID), each crossed with all three C variants. No standalone-improvement gate. Selection stops if fewer candidates are feasible; unused slots are not transferred.

Event:16 cases: choose two best feasible programs per family AC, AD, CD and AA (AA includes both sharing modes), each crossed with E01 and E05. No strict improvement gate. Use newly generated native timings and fixed native engine choices, never published980 timing. Each compaction is freshly allocated and checked. Retain the unretimed same-run counterpart.

Triple:4 cases: two best feasible plain AC pairs, each with D01 and D16. These are bounded tests of higher-order coupling, not a Cartesian sweep.

Validation:at most8 fresh reconstructions for up to two candidates at or below980. No transfer between family caps. Total ceiling100 counted builds/reconstructions, including four controls and conditional validation. Reserve before work, source-hash check, append-only unique attempts, no retries, stop family on semantic failure. The total is 4+12+40+10+6+16+4+8=100.

## Oracle and promotion rules

All setup and runtime work is charged; one core, VLEN8, ALU12/VALU6/load2/store2/flow1. Reject scratch>1536 BEFORE lowering or execution. Do not generate using runtime tree/input values. Root-start and final-output-value semantics, unchanged non-output memory, frozen reference execution.

Every feasible candidate: independent lane/capacity and complete logical dependency checks, three paired full-width seeds, actual complete cycles. Select family finalists for five asymmetric patterns. Compare both absolute cycles and interaction: I = T(AB)-T(A)-T(B)+T(base); negative I means a favorable interaction relative to additive cycle changes, not necessarily a speed win. Record improvement versus each component and versus baseline981/published980. For shared setup and event compaction use directly matched S-off/unretimed controls, not an additive claim without the necessary cells.

At most two candidates <=980 advance to100 full-width seeds, five patterns and fresh deterministic reconstructions. Any claimed new best below980 or below experimental980/1449 also requires nine frozen tests, supplemental/legacy/fallback/pause-store gates, corruption sensitivity and isolated source-only oracle-blocked generation. Until those pass, label it a candidate, not a new incumbent. No production-readiness claim without automatic source-only discovery and separately authorized promotion.

No solver queries. Unknown is not UNSAT. Structural/engine bounds are scoped to the retained work. Archive all negatives and numerical interactions; do not present scratch-rejected timing as executed performance. Close every budget at completion, including unused slots.
